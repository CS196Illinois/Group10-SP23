use pwd and cd to .../project/
git add FrontEnd/*file name*  (more slashes if more folders like client)
git commit -m"message"
git push

------------------

if no new files:

git commit -am"message"
git push